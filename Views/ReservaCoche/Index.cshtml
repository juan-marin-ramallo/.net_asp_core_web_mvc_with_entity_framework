@model IEnumerable<WebApplicationMVC.Models.ReservaCoche>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GalonesGasolina)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NumeroReservaNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PlacaNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GalonesGasolina)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumeroReservaNavigation.NumeroReserva)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PlacaNavigation.Placa)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
